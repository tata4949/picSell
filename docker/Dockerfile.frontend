FROM node:20-alpine

WORKDIR /app

# Copy package files from the build context (frontend) and install
COPY package*.json ./
RUN npm install

# Copy application source from the build context
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]
